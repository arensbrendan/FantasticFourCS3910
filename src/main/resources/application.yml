server:
  port: 8080

spring:
  application:
    name: commerce-service

  h2:
    console:
      enabled: true
      settings:
        web-allow-others: true
      path: /h2-console

  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:file:~/commerce_bank_db
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: update
